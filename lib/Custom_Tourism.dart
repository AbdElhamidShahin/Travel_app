import 'package:flutter/material.dart';

class CustomTourism extends StatelessWidget {
  const CustomTourism({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [

      ],
    );
  }
}
